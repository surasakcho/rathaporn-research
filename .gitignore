.venv/

# Raw raster tiles — large source downloads, not for version control
# Only the mosaics (Thailand-DEM.tif, Thailand-slope.tif) are kept in LFS
**/data/raw/elevation-slope/AW3D30-*.tif
**/data/raw/elevation-slope/redownload/
**/data/raw/งาน RA ครั้งที่ 2/

# Intermediate rasters — reproducible by re-running terrain_vars.py
**/data/processed/_terrain_intermediate/

# GDAL/QGIS stats cache — auto-generated, not source data
*.aux.xml
